"""
top-k 호출에 사용하는 비즈니스 로직을 담은 코드 페이지입니다.
"""


def get_top_k(query_message: str):
    """
    query message로부터 top-k를 가져와 반환합니다.
    """

    answer = f"response of {query_message}"
    return answer
